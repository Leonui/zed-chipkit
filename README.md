# zed-chipkit

Zed extension that adds file associations and syntax highlighting for:

- Verilog (`.v`)
- SystemVerilog (`.sv`, `.svh`, `.vh`)
- Tcl (`.tcl`)
- SKILL (`.il`, `.ils`, `.sk`)
- UPF (`.upf`)
- CPF (`.cpf`)
- SDC (`.sdc`)
